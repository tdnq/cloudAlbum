const host="http://172.16.152.204:3000";
const albumApi=`${host}/api/album`;
const photoApi =`${host}/api/photo`;
const userLogInApi=`${host}/api/user/logIn`;
module.exports={
  host,
  albumApi,
  photoApi,
  userLogInApi
}